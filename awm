#!/usr/bin/env python3
"""Standalone launcher so the CLI works without pip-installing the package."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from agent_worktree_manager.cli import main

sys.exit(main())
